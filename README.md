# Dhruv-Sharma
The document is standardized, text and coordinates are extracted, the search phrase is detected through direct and sequential matching, bounding boxes are drawn around each instance, and a new annotated file is generated without altering the original.
